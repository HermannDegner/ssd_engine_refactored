# SSD Theory - Experimental

実験的機能 - Nano Coreエンジン

## ⚠️ 実験的実装

このフォルダの実装は**研究・検証段階**です：

- API安定性: 低
- 本番使用: 非推奨
- ドキュメント: 不完全
- テスト: 限定的

## 📦 ファイル一覧

### **nano_core_engine.py** (21.0KB) ⭐ 最新
Nano Core Engine - 超軽量SSD実装

**特徴:**
- 最小限のコード
- 高速な動作
- 簡潔なAPI
- プロトタイピング向け

**用途:**
- 概念検証
- 高速プロトタイピング
- 教育・学習
- パフォーマンステスト

**制限:**
- 機能が限定的
- 拡張性が低い
- エラーハンドリング簡略
- ドキュメント不足

### **nano_core_engine_v9.py** (19.4KB) - 旧版
V9時点のNano Core

**状態:** 非推奨、アーカイブ目的で保管

**使用すべきでない理由:**
- バグ修正が反映されていない
- 古いAPI設計
- 最新理論との不整合
- メンテナンス停止

## 🚀 使用方法

### 基本的な使用

```python
from ssd_engine_refactored.experimental import NanoCoreEngine

# エンジン作成
engine = NanoCoreEngine()

# 圧力入力
engine.add_pressure(10.0)

# 状態更新
engine.step()

# アクション取得
action = engine.get_action()
```

### 高速プロトタイプ作成

```python
# シンプルなゲームAI
class SimpleGameAI:
    def __init__(self):
        self.engine = NanoCoreEngine()
    
    def decide_action(self, game_state):
        # 状況を圧力に変換
        pressure = self.calculate_pressure(game_state)
        
        # Nano Coreで処理
        self.engine.add_pressure(pressure)
        self.engine.step()
        
        return self.engine.get_action()
```

## 🔬 研究用途

### 概念検証

新しいアイデアを素早く実装・テスト：

```python
# 新しい圧力計算アルゴリズムのテスト
def new_pressure_algorithm(state):
    # 実験的な計算
    return experimental_value

engine = NanoCoreEngine()
for state in test_cases:
    engine.add_pressure(new_pressure_algorithm(state))
    engine.step()
    results.append(engine.get_action())
```

### パフォーマンス測定

```python
import time

# Nano Coreの速度測定
start = time.time()
for _ in range(10000):
    engine.step()
elapsed = time.time() - start

print(f"10000ステップ: {elapsed:.3f}秒")
```

## 🆚 Core vs Nano Core

| 特徴 | Core | Nano Core |
|------|------|-----------|
| 機能性 | ⭐⭐⭐ | ⭐ |
| 拡張性 | ⭐⭐⭐ | ⭐ |
| 速度 | ⭐⭐ | ⭐⭐⭐ |
| 安定性 | ⭐⭐⭐ | ⭐⭐ |
| ドキュメント | ⭐⭐⭐ | ⭐ |
| 本番使用 | ✅ | ❌ |
| 学習用 | ✅ | ✅ |
| プロトタイプ | ⭐⭐ | ⭐⭐⭐ |

## 📈 ロードマップ

### 現在の状態
- [x] 基本機能実装
- [x] 動作検証
- [ ] 包括的テスト
- [ ] 完全なドキュメント
- [ ] API安定化

### 将来の方向性

**選択肢1: 昇格**
- 十分な検証後、core/に統合
- API安定化
- 完全なドキュメント作成

**選択肢2: 維持**
- 実験的機能として継続
- 新アイデアのテストベッド
- 学習用途に特化

**選択肢3: 廃止**
- core/で十分な場合
- メンテナンスコスト高
- 別実装で代替可能

## ⚡ クイックリファレンス

### 主要API

```python
engine = NanoCoreEngine()

# 圧力追加
engine.add_pressure(value)

# 状態更新
engine.step()

# アクション取得
action = engine.get_action()

# リセット
engine.reset()

# 状態確認
print(engine.get_state())
```

## 🔗 関連リソース

- `../core/` - 安定版実装
- `../examples/demos/` - 基本デモ
- `../docs/` - 理論ドキュメント

## ⚠️ 重要な注意

### 本番使用しないこと

実験的実装のため：
- バグの可能性
- API変更の可能性
- サポート限定的
- ドキュメント不完全

### 代わりに使用すべきもの

```python
# ❌ 実験版
from ssd_engine_refactored.experimental import NanoCoreEngine

# ✅ 安定版
from ssd_engine_refactored.core import HumanAgent
```

---

*Note: 実験的機能 - 自己責任で使用してください*
